# grocery-tracker
This contains my CS 210 Project Three (Corner Grocer). The program reads grocery item data from an input file, tracks how often each item appears using a map, and allows the user to search for items, display all frequencies, and view a histogram through a menu-driven interface.
